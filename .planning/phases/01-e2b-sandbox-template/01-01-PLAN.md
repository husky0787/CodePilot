---
phase: 01-e2b-sandbox-template
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/health/route.ts
  - sandbox/e2b.Dockerfile
  - sandbox/start.sh
autonomous: true
requirements:
  - SAND-01
  - SAND-02
  - SAND-03

must_haves:
  truths:
    - "健康检查端点验证 SQLite 可用性和 Claude CLI 可发现性"
    - "Dockerfile 预装 better-sqlite3 编译工具链、项目依赖、Claude Code CLI"
    - "启动脚本以 0.0.0.0 绑定启动 Next.js 并轮询健康检查直到就绪"
  artifacts:
    - path: "src/app/api/health/route.ts"
      provides: "深度健康检查（SQLite + Claude CLI）"
      contains: "getDb|findClaudeBinary"
    - path: "sandbox/e2b.Dockerfile"
      provides: "E2B 沙箱模板定义"
      contains: "e2bdev/base"
    - path: "sandbox/start.sh"
      provides: "沙箱启动脚本"
      contains: "hostname 0.0.0.0"
  key_links:
    - from: "sandbox/start.sh"
      to: "/api/health"
      via: "curl 轮询健康检查端点"
      pattern: "curl.*api/health"
    - from: "sandbox/e2b.Dockerfile"
      to: "sandbox/start.sh"
      via: "COPY 和 chmod"
      pattern: "COPY.*start\\.sh"
---

<objective>
创建 E2B 沙箱模板所需的全部文件：增强健康检查端点、编写 Dockerfile 和启动脚本。

Purpose: 构成 E2B 自定义沙箱模板的核心产物，使 CodePilot 可在沙箱中一键启动
Output: 增强的 /api/health 端点、sandbox/e2b.Dockerfile、sandbox/start.sh
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-e2b-sandbox-template/01-CONTEXT.md
@.planning/phases/01-e2b-sandbox-template/01-RESEARCH.md

@src/app/api/health/route.ts
@src/lib/platform.ts
@src/lib/db.ts
</context>

<interfaces>
<!-- 执行器需要了解的现有接口 -->

From src/app/api/health/route.ts:
```typescript
// 当前实现 — 仅返回 { status: 'ok' }，需增强为深度检查
export async function GET()
```

From src/lib/platform.ts:
```typescript
export function findClaudeBinary(): string | undefined;
// 在 Linux 上搜索: /usr/local/bin/claude, /usr/bin/claude, ~/.npm-global/bin/claude, ~/.local/bin/claude, ~/.claude/bin/claude
// 以及 `which claude` fallback
```

From src/lib/db.ts:
```typescript
export function getDb(): BetterSqlite3.Database;
// 数据库路径通过 CLAUDE_GUI_DATA_DIR 环境变量可配
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 增强健康检查端点</name>
  <files>src/app/api/health/route.ts</files>
  <action>
增强现有 `/api/health` 路由，从当前简单的 `{ status: 'ok' }` 改为深度健康检查：

1. 导入 `getDb` from `@/lib/db` 和 `findClaudeBinary` from `@/lib/platform`
2. 在 try/catch 中依次检查：
   - SQLite: 调用 `getDb()` 并执行 `db.prepare('SELECT 1').get()` 验证数据库可读写
   - Claude CLI: 调用 `findClaudeBinary()` 检查 CLI 二进制是否可发现
3. 全部通过时返回 200: `{ status: 'ok', sqlite: true, claude_cli: true, claude_path: string }`
4. 任一检查失败时返回 503: `{ status: 'error', message: string }`
5. 用 try/catch 包裹每个检查，确保单个检查失败不会导致未捕获异常

注意：保持 GET export，不修改路由路径。`findClaudeBinary()` 在沙箱外（开发机器上）也能工作，所以本地 `npm run test` 不会因此失败。如果 `findClaudeBinary()` 返回 undefined，仍返回 200 但 `claude_cli: false`，因为在本地开发环境 CLI 可能未安装。只有 SQLite 失败才返回 503。
  </action>
  <verify>
    <automated>cd /home/user_demo/Husky/Codepilot-3 && npm run test</automated>
  </verify>
  <done>GET /api/health 返回 SQLite 和 Claude CLI 状态信息，SQLite 失败时返回 503</done>
</task>

<task type="auto">
  <name>Task 2: 创建 E2B 沙箱模板文件</name>
  <files>sandbox/e2b.Dockerfile, sandbox/start.sh</files>
  <action>
创建 `sandbox/` 目录并编写两个核心文件：

**sandbox/e2b.Dockerfile:**
```dockerfile
FROM e2bdev/base:latest

# 安装 better-sqlite3 编译工具链
RUN apt-get update && apt-get install -y \
    build-essential \
    python3 \
    g++ \
    make \
    && rm -rf /var/lib/apt/lists/*

# 设置工作目录（E2B 默认用户为 user）
WORKDIR /home/user/codepilot

# 复制 package.json 和 lock 文件（利用 Docker 层缓存）
COPY package.json package-lock.json ./

# 安装依赖（包括 better-sqlite3 native 编译）
RUN npm install

# 复制项目源码
COPY . .

# 预装 Claude Code CLI（固定版本，更新时重建模板）
RUN npm install -g @anthropic-ai/claude-code@1.0.17

# 预创建 Claude 配置目录，避免首次运行时的权限问题
RUN mkdir -p /home/user/.claude /home/user/.codepilot

# 设置数据目录环境变量
ENV CLAUDE_GUI_DATA_DIR=/home/user/.codepilot

# 复制启动脚本
COPY sandbox/start.sh /home/user/start.sh
RUN chmod +x /home/user/start.sh
```

**sandbox/start.sh:**
```bash
#!/bin/bash
set -e

cd /home/user/codepilot

# 启动 Next.js dev server（后台运行，绑定 0.0.0.0 供外部访问）
npm run dev -- --hostname 0.0.0.0 &

# 轮询健康检查端点，确认 Next.js + SQLite + Claude CLI 全部就绪
MAX_WAIT=60
WAITED=0
until curl -sf http://localhost:3000/api/health > /dev/null 2>&1; do
  if [ $WAITED -ge $MAX_WAIT ]; then
    echo "ERROR: CodePilot failed to start within ${MAX_WAIT}s"
    exit 1
  fi
  sleep 1
  WAITED=$((WAITED + 1))
done

echo "CodePilot is ready (took ${WAITED}s)"

# 预热首页，触发 Next.js 按需编译，避免用户首次访问等待
curl -sf http://localhost:3000/ > /dev/null 2>&1 || true

# 保持前台运行（防止沙箱认为进程结束）
wait
```

注意事项：
- Dockerfile 使用 `e2bdev/base:latest` 基础镜像（用户锁定决定）
- 使用 dev server 模式（用户锁定决定），不做 `npm run build`
- Claude CLI 版本固定在 Dockerfile 中（用户锁定决定）
- `--hostname 0.0.0.0` 通过 CLI 参数实现（用户锁定决定），不修改 next.config.ts
- start.sh 必须有 LF 行尾（不能是 CRLF），确保在 Linux 沙箱中可执行
  </action>
  <verify>
    <automated>test -f /home/user_demo/Husky/Codepilot-3/sandbox/e2b.Dockerfile && test -f /home/user_demo/Husky/Codepilot-3/sandbox/start.sh && head -1 /home/user_demo/Husky/Codepilot-3/sandbox/start.sh | grep -q '#!/bin/bash' && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>sandbox/e2b.Dockerfile 和 sandbox/start.sh 存在，Dockerfile 基于 e2bdev/base 并预装所有依赖，start.sh 绑定 0.0.0.0 并轮询健康检查</done>
</task>

</tasks>

<verification>
- `npm run test` 通过（健康检查端点改动不破坏类型检查和单元测试）
- sandbox/e2b.Dockerfile 存在且包含 `e2bdev/base`、`build-essential`、`npm install`、`claude-code`
- sandbox/start.sh 存在且包含 `--hostname 0.0.0.0`、`/api/health` 轮询、`wait`
</verification>

<success_criteria>
1. /api/health 端点返回 SQLite 和 Claude CLI 状态
2. Dockerfile 预装完整工具链和依赖
3. 启动脚本正确绑定 0.0.0.0 并做健康检查轮询
</success_criteria>

<output>
After completion, create `.planning/phases/01-e2b-sandbox-template/01-01-SUMMARY.md`
</output>
