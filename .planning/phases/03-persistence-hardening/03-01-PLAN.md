---
phase: 03-persistence-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/site/src/lib/e2b.ts
  - apps/site/src/lib/sandbox-storage.ts
  - apps/site/src/lib/rate-limit.ts
  - apps/site/src/app/api/sandbox/heartbeat/route.ts
  - apps/site/src/app/api/sandbox/pause/route.ts
  - apps/site/src/app/api/sandbox/resume/route.ts
  - apps/site/src/app/api/sandbox/create/route.ts
  - apps/site/src/app/api/sandbox/status/route.ts
  - apps/site/src/components/cloud/SandboxRestore.tsx
  - apps/site/src/components/cloud/IdleWarningBanner.tsx
  - apps/site/src/components/cloud/CloudLauncher.tsx
  - apps/site/src/__tests__/sandbox-pause.test.ts
  - apps/site/src/__tests__/sandbox-resume.test.ts
  - apps/site/src/__tests__/sandbox-heartbeat.test.ts
  - apps/site/src/__tests__/rate-limit.test.ts
autonomous: true
requirements: [LIFE-01]

must_haves:
  truths:
    - "Sandbox auto-pauses after 15 minutes of user inactivity"
    - "User sees countdown warning 2 minutes before auto-pause"
    - "User can resume a paused sandbox and all files/config are preserved"
    - "Sandbox has 24-hour maximum lifetime with warning before expiry"
    - "IP-level rate limiting prevents sandbox creation abuse"
    - "Sandbox creation/resume retries 1-2 times on failure"
  artifacts:
    - path: "apps/site/src/lib/e2b.ts"
      provides: "pauseSandbox, resumeSandbox functions"
      exports: ["pauseSandbox", "resumeSandbox", "createSandbox", "checkSandbox"]
    - path: "apps/site/src/lib/rate-limit.ts"
      provides: "IP rate limiting utility"
      exports: ["checkRateLimit"]
    - path: "apps/site/src/app/api/sandbox/heartbeat/route.ts"
      provides: "Heartbeat API that tracks idle time and renews E2B timeout"
      exports: ["POST"]
    - path: "apps/site/src/app/api/sandbox/pause/route.ts"
      provides: "Manual pause API"
      exports: ["POST"]
    - path: "apps/site/src/app/api/sandbox/resume/route.ts"
      provides: "Resume paused sandbox API"
      exports: ["POST"]
    - path: "apps/site/src/components/cloud/IdleWarningBanner.tsx"
      provides: "Countdown warning banner before auto-pause"
    - path: "apps/site/src/components/cloud/SandboxRestore.tsx"
      provides: "Pause-aware sandbox restore with resume support"
  key_links:
    - from: "apps/site/src/components/cloud/CloudLauncher.tsx"
      to: "/api/sandbox/heartbeat"
      via: "setInterval heartbeat every 60s"
      pattern: "fetch.*api/sandbox/heartbeat"
    - from: "apps/site/src/app/api/sandbox/heartbeat/route.ts"
      to: "apps/site/src/lib/e2b.ts"
      via: "sandbox.setTimeout() to renew E2B timeout"
      pattern: "sandbox\\.setTimeout"
    - from: "apps/site/src/app/api/sandbox/pause/route.ts"
      to: "apps/site/src/lib/e2b.ts"
      via: "pauseSandbox() with sync + pause"
      pattern: "pauseSandbox"
    - from: "apps/site/src/app/api/sandbox/create/route.ts"
      to: "apps/site/src/lib/rate-limit.ts"
      via: "checkRateLimit before creating sandbox"
      pattern: "checkRateLimit"
---

<objective>
Implement sandbox lifecycle management: heartbeat-based idle detection, auto-pause after 15 minutes, resume with state preservation, idle warning UI, rate limiting, and production hardening (retry logic, 24h max lifetime).

Purpose: LIFE-01 requires sandboxes to auto-pause on idle and resume with full data integrity. This prevents runaway E2B costs and provides seamless session continuity.
Output: Backend APIs (heartbeat/pause/resume), rate limiting, idle warning banner, pause-aware sandbox restore, unit tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-persistence-hardening/03-CONTEXT.md
@.planning/phases/03-persistence-hardening/03-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From apps/site/src/lib/e2b.ts:
```typescript
import { Sandbox } from "e2b";
export const TEMPLATE_ID = "9114lthidrvmoik0fcdw";
export async function createSandbox(anthropicKey?: string): Promise<{ sandboxId: string; url: string }>;
export async function checkSandbox(sandboxId: string): Promise<{ alive: boolean; ready: boolean; url?: string }>;
```

From apps/site/src/lib/sandbox-storage.ts:
```typescript
export interface SavedSandbox {
  sandboxId: string;
  url: string;
  createdAt: number;
}
export function saveSandbox(info: SavedSandbox): void;
export function loadSandbox(): SavedSandbox | null;
export function clearSandbox(): void;
```

From apps/site/src/app/api/sandbox/create/route.ts:
```typescript
export const maxDuration = 60;
export async function POST(req: NextRequest): NextResponse; // accepts { apiKey?: string }
```

From apps/site/src/app/api/sandbox/status/route.ts:
```typescript
export async function GET(req: NextRequest): NextResponse; // query param: id
```

From apps/site/src/components/cloud/SandboxRestore.tsx:
```typescript
interface SandboxRestoreProps { onCreateNew: () => void; }
type RestoreState = { status: "checking" } | { status: "found"; url: string } | { status: "none" };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Library functions — e2b pause/resume + rate limiting + tests</name>
  <files>
    apps/site/src/lib/e2b.ts,
    apps/site/src/lib/rate-limit.ts,
    apps/site/src/__tests__/sandbox-pause.test.ts,
    apps/site/src/__tests__/sandbox-resume.test.ts,
    apps/site/src/__tests__/rate-limit.test.ts
  </files>
  <behavior>
    - pauseSandbox: connects to sandbox, runs `sync`, calls pause(), returns true on success, false on error
    - resumeSandbox: connects with 30min timeout, returns { sandboxId, url } or null on failure; retries once on transient error
    - checkRateLimit(ip, maxPerHour): returns true if under limit, false if over; resets counter after 1 hour; cleans up expired entries
  </behavior>
  <action>
    1. Extend `apps/site/src/lib/e2b.ts`:
       - Add `pauseSandbox(sandboxId: string): Promise<boolean>` — `Sandbox.connect(sandboxId)` -> `sandbox.commands.run("sync")` -> `sandbox.pause()` (try `pause()` first, fallback to `betaPause()` if not a function). Return true on success, false + console.error on failure.
       - Add `resumeSandbox(sandboxId: string): Promise<{ sandboxId: string; url: string } | null>` — `Sandbox.connect(sandboxId, { timeoutMs: 30 * 60 * 1000 })` -> return `{ sandboxId, url: "https://" + sandbox.getHost(3000) }`. On failure return null. Include 1 retry with 2s delay for transient errors.
       - Update `checkSandbox` to distinguish paused vs dead: try connect, if succeeds -> alive. For paused detection, catch connect errors and check if sandbox exists via `Sandbox.list()` — if found in list but connect fails, it's paused.

    2. Create `apps/site/src/lib/rate-limit.ts`:
       - In-memory Map<string, { count: number; resetAt: number }>
       - `checkRateLimit(ip: string, maxPerHour = 5): boolean` — returns true if allowed, increments count
       - Periodic cleanup every 60s via module-level setInterval to prevent memory leak
       - Export for testing: `_resetForTest()` to clear the map

    3. Write unit tests using `node:test` + `mock.module` pattern (matching Phase 2 test conventions):
       - `sandbox-pause.test.ts`: mock e2b Sandbox, verify sync + pause called, verify error handling returns false
       - `sandbox-resume.test.ts`: mock e2b Sandbox, verify connect called with 30min timeout, verify retry on first failure, verify null returned on persistent failure
       - `rate-limit.test.ts`: verify allows up to N requests, blocks N+1, resets after expiry

    Note: Tests use `node --experimental-strip-types --experimental-test-module-mocks --test` (per Phase 2 convention, NOT tsx for mock.module compatibility).
  </action>
  <verify>
    <automated>cd /home/user_demo/Husky/Codepilot-3 && node --experimental-strip-types --experimental-test-module-mocks --test apps/site/src/__tests__/sandbox-pause.test.ts apps/site/src/__tests__/sandbox-resume.test.ts apps/site/src/__tests__/rate-limit.test.ts</automated>
  </verify>
  <done>
    - pauseSandbox runs sync + pause on E2B sandbox, returns boolean
    - resumeSandbox connects + returns URL, retries once on failure
    - checkRateLimit allows up to N/hour, blocks excess
    - All 3 test files pass (pause, resume, rate-limit)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: API routes — heartbeat, pause, resume endpoints + create hardening + heartbeat test</name>
  <files>
    apps/site/src/app/api/sandbox/heartbeat/route.ts,
    apps/site/src/app/api/sandbox/pause/route.ts,
    apps/site/src/app/api/sandbox/resume/route.ts,
    apps/site/src/app/api/sandbox/create/route.ts,
    apps/site/src/__tests__/sandbox-heartbeat.test.ts
  </files>
  <behavior>
    - POST /api/sandbox/heartbeat: accepts { sandboxId }, calls sandbox.setTimeout(30min), returns { status: "ok" } or { status: "paused_or_dead" }
    - POST /api/sandbox/pause: accepts { sandboxId }, calls pauseSandbox, returns success/error
    - POST /api/sandbox/resume: accepts { sandboxId }, calls resumeSandbox with retry, returns { sandboxId, url } or error
    - POST /api/sandbox/create: adds IP rate limiting (checkRateLimit), returns 429 if over limit; retries once on E2B failure
  </behavior>
  <action>
    1. Create `apps/site/src/app/api/sandbox/heartbeat/route.ts`:
       - POST handler: parse `{ sandboxId }` from body, validate non-empty
       - `Sandbox.connect(sandboxId)` -> `sandbox.setTimeout(30 * 60 * 1000)` to renew E2B timeout
       - Return `{ status: "ok" }` on success, `{ status: "paused_or_dead" }` on connect failure
       - Set `maxDuration = 15` (heartbeat should be fast)

    2. Create `apps/site/src/app/api/sandbox/pause/route.ts`:
       - POST handler: parse `{ sandboxId }`, call `pauseSandbox(sandboxId)` from e2b.ts
       - Return `{ success: true }` or `{ error: "..." }` with 500

    3. Create `apps/site/src/app/api/sandbox/resume/route.ts`:
       - POST handler: parse `{ sandboxId }`, call `resumeSandbox(sandboxId)` from e2b.ts
       - Return `{ sandboxId, url }` on success, `{ error: "..." }` with 500
       - Set `maxDuration = 60` (resume can be slow)

    4. Update `apps/site/src/app/api/sandbox/create/route.ts`:
       - Import `checkRateLimit` from rate-limit.ts
       - Before creating sandbox: get IP from `req.headers.get("x-forwarded-for")` or `req.ip` or "unknown"
       - If `!checkRateLimit(ip, 5)` return 429 with `{ error: "Rate limit exceeded" }`
       - Add retry logic: if `createSandbox()` throws, retry once after 2s delay, then return 500

    5. Write `sandbox-heartbeat.test.ts`:
       - Mock e2b Sandbox, verify setTimeout called with correct value (30 * 60 * 1000)
       - Verify returns "paused_or_dead" when connect fails
  </action>
  <verify>
    <automated>cd /home/user_demo/Husky/Codepilot-3 && node --experimental-strip-types --experimental-test-module-mocks --test apps/site/src/__tests__/sandbox-heartbeat.test.ts</automated>
  </verify>
  <done>
    - heartbeat API renews E2B timeout, returns status
    - pause/resume APIs call e2b.ts functions correctly
    - create API rejects requests over 5/hour/IP with 429
    - create API retries once on E2B failure
    - heartbeat test passes
  </done>
</task>

<task type="auto">
  <name>Task 3: Sandbox storage extension + idle warning + heartbeat hook + SandboxRestore update</name>
  <files>
    apps/site/src/lib/sandbox-storage.ts,
    apps/site/src/components/cloud/IdleWarningBanner.tsx,
    apps/site/src/components/cloud/SandboxRestore.tsx,
    apps/site/src/components/cloud/CloudLauncher.tsx
  </files>
  <action>
    1. Extend `apps/site/src/lib/sandbox-storage.ts`:
       - Add `paused?: boolean` and `pausedAt?: number` fields to `SavedSandbox` interface
       - Add `updateSandboxPaused(paused: boolean): void` — loads existing, updates paused/pausedAt, saves back
       - Existing loadSandbox/saveSandbox/clearSandbox remain backward compatible

    2. Create `apps/site/src/components/cloud/IdleWarningBanner.tsx`:
       - Fixed-position banner at top of viewport (z-50), yellow/amber background
       - Props: `secondsRemaining: number`, `onExtend: () => void`, `onDismiss: () => void`
       - Shows countdown text: "Sandbox will pause in {mm:ss} due to inactivity"
       - "Stay Active" button calls onExtend (sends heartbeat to reset timer)
       - Only renders when secondsRemaining > 0
       - Framer-motion slide-down animation (matching existing project style)

    3. Update `apps/site/src/components/cloud/SandboxRestore.tsx`:
       - Extend RestoreState: add `| { status: "paused"; sandboxId: string }`
       - In check(): if saved.paused is true, set status to "paused" without trying to connect
       - If saved.paused is false but checkSandbox returns not alive, try checking if it's paused (call `/api/sandbox/status?id=...` which now returns paused state)
       - Add "Resume Paused Sandbox" button that calls POST /api/sandbox/resume, shows loading spinner, then redirects to returned URL
       - On resume failure: show error message, offer "Create New Sandbox" option

    4. Update `apps/site/src/components/cloud/CloudLauncher.tsx`:
       - After sandbox is launched and user is redirected, start heartbeat interval:
         - Every 60 seconds, POST /api/sandbox/heartbeat with { sandboxId }
         - If response is "paused_or_dead", stop heartbeat and show notification
       - Track idle locally: reset a 15-minute countdown on any user interaction (mousemove, keydown, click, scroll — debounced to 1 event per 5s)
       - When 13 minutes of idle (2 minutes before pause): show IdleWarningBanner with countdown
       - When "Stay Active" clicked: send heartbeat, reset idle timer, hide banner
       - 24-hour max lifetime: track sandbox creation time, show warning at 23h ("Sandbox will shut down in 1 hour, please save your work"), force pause at 24h via POST /api/sandbox/pause
       - On pause (auto or forced): call updateSandboxPaused(true), show message to user

    Note: The heartbeat + idle detection logic runs in the portal site (apps/site), NOT inside the CodePilot sandbox. The portal page that launched/redirected the user may be closed — so heartbeat should also be sent from inside the sandbox via a lightweight script. However, per CONTEXT.md the decision is "CodePilot frontend sends heartbeat to portal API". Since the user is redirected to the sandbox URL and the portal tab may close, implement heartbeat as: (a) portal CloudLauncher sends heartbeat while its tab is open, AND (b) add a note that a future enhancement could add in-sandbox heartbeat. For now, rely on E2B's own timeout as the fallback if the portal tab is closed.
  </action>
  <verify>
    <automated>cd /home/user_demo/Husky/Codepilot-3 && npm run test</automated>
  </verify>
  <done>
    - SavedSandbox interface includes paused/pausedAt fields
    - IdleWarningBanner renders countdown with "Stay Active" button
    - SandboxRestore detects paused sandboxes and offers resume
    - CloudLauncher sends heartbeat every 60s, shows idle warning at 13min, triggers pause at 15min
    - 24-hour max lifetime warning and forced pause implemented
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 4: Status API enhancement + cleanup logic</name>
  <files>
    apps/site/src/app/api/sandbox/status/route.ts
  </files>
  <action>
    1. Update `apps/site/src/app/api/sandbox/status/route.ts`:
       - Import `Sandbox` from "e2b" directly for listing
       - Enhanced response: `{ alive: boolean; ready: boolean; paused: boolean; url?: string; age?: number }`
       - To detect paused state: if `checkSandbox` returns not alive, call `Sandbox.list()` and check if sandboxId appears in the list — if yes, it's likely paused (E2B lists paused sandboxes)
       - If sandbox is paused, return `{ alive: false, ready: false, paused: true }`
       - If sandbox truly doesn't exist, return `{ alive: false, ready: false, paused: false }`
       - Add `age` field: calculate from sandbox metadata or from the createdAt stored in localStorage (passed as query param)

    2. Lazy cleanup of expired sandboxes: when status check finds a sandbox older than 24 hours that's still running, call pauseSandbox to force-pause it. This is "cleanup on access" per Claude's discretion from CONTEXT.md.
  </action>
  <verify>
    <automated>cd /home/user_demo/Husky/Codepilot-3 && npm run test</automated>
  </verify>
  <done>
    - Status API returns paused field distinguishing paused vs dead sandboxes
    - Expired sandboxes (>24h) are force-paused on status check
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
1. `npm run test` passes (typecheck + unit tests)
2. Unit tests for pause/resume/heartbeat/rate-limit all pass
3. All new API routes compile and export correct HTTP methods
4. sandbox-storage.ts backward compatible (existing loadSandbox still works)
</verification>

<success_criteria>
- Heartbeat API accepts sandboxId and renews E2B timeout
- Pause API calls sync + pause on sandbox
- Resume API reconnects to paused sandbox and returns URL
- Rate limiting blocks >5 sandbox creations per IP per hour
- Create API retries once on E2B failure
- Idle warning banner appears 2 minutes before auto-pause
- SandboxRestore distinguishes paused vs dead sandboxes
- Status API returns paused state
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-persistence-hardening/03-01-SUMMARY.md`
</output>
