---
phase: 03-persistence-hardening
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - apps/site/src/lib/sandbox-storage.ts
  - apps/site/src/components/cloud/IdleWarningBanner.tsx
  - apps/site/src/components/cloud/SandboxRestore.tsx
  - apps/site/src/components/cloud/CloudLauncher.tsx
  - apps/site/src/app/api/sandbox/status/route.ts
autonomous: true
requirements: [LIFE-01]

must_haves:
  truths:
    - "Sandbox auto-pauses after 15 minutes of user inactivity"
    - "User sees countdown warning 2 minutes before auto-pause"
    - "User can resume a paused sandbox and all files/config are preserved"
    - "Sandbox has 24-hour maximum lifetime with warning before expiry"
    - "Status API distinguishes paused vs dead sandboxes"
  artifacts:
    - path: "apps/site/src/components/cloud/IdleWarningBanner.tsx"
      provides: "Countdown warning banner before auto-pause"
    - path: "apps/site/src/components/cloud/SandboxRestore.tsx"
      provides: "Pause-aware sandbox restore with resume support"
    - path: "apps/site/src/app/api/sandbox/status/route.ts"
      provides: "Enhanced status API with paused state detection"
      exports: ["GET"]
  key_links:
    - from: "apps/site/src/components/cloud/CloudLauncher.tsx"
      to: "/api/sandbox/heartbeat"
      via: "setInterval heartbeat every 60s"
      pattern: "fetch.*api/sandbox/heartbeat"
    - from: "apps/site/src/components/cloud/SandboxRestore.tsx"
      to: "/api/sandbox/resume"
      via: "POST to resume paused sandbox"
      pattern: "fetch.*api/sandbox/resume"
    - from: "apps/site/src/app/api/sandbox/status/route.ts"
      to: "apps/site/src/lib/e2b.ts"
      via: "Sandbox.list() to detect paused state"
      pattern: "Sandbox\\.list"
---

<objective>
Implement frontend sandbox lifecycle UI: idle warning banner, heartbeat integration in CloudLauncher, pause-aware SandboxRestore, enhanced status API with paused-state detection, and 24h max lifetime enforcement.

Purpose: This plan wires the backend APIs (from Plan 01) into the user-facing components so users see idle warnings, can resume paused sandboxes, and experience graceful lifecycle management.
Output: IdleWarningBanner component, updated CloudLauncher with heartbeat + idle detection, updated SandboxRestore with resume flow, enhanced status API.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-persistence-hardening/03-CONTEXT.md
@.planning/phases/03-persistence-hardening/03-RESEARCH.md
@.planning/phases/03-persistence-hardening/03-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 that this plan consumes. -->

From apps/site/src/lib/e2b.ts (extended by Plan 01):
```typescript
export async function pauseSandbox(sandboxId: string): Promise<boolean>;
export async function resumeSandbox(sandboxId: string): Promise<{ sandboxId: string; url: string } | null>;
export async function checkSandbox(sandboxId: string): Promise<{ alive: boolean; ready: boolean; url?: string }>;
```

From apps/site/src/lib/sandbox-storage.ts:
```typescript
export interface SavedSandbox {
  sandboxId: string;
  url: string;
  createdAt: number;
}
export function saveSandbox(info: SavedSandbox): void;
export function loadSandbox(): SavedSandbox | null;
export function clearSandbox(): void;
```

API routes created by Plan 01:
```
POST /api/sandbox/heartbeat  { sandboxId } -> { status: "ok" | "paused_or_dead" }
POST /api/sandbox/pause      { sandboxId } -> { success: true } | { error: string }
POST /api/sandbox/resume     { sandboxId } -> { sandboxId, url } | { error: string }
```

From apps/site/src/components/cloud/SandboxRestore.tsx:
```typescript
interface SandboxRestoreProps { onCreateNew: () => void; }
type RestoreState = { status: "checking" } | { status: "found"; url: string } | { status: "none" };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sandbox storage extension + idle warning + heartbeat hook + SandboxRestore update</name>
  <files>
    apps/site/src/lib/sandbox-storage.ts,
    apps/site/src/components/cloud/IdleWarningBanner.tsx,
    apps/site/src/components/cloud/SandboxRestore.tsx,
    apps/site/src/components/cloud/CloudLauncher.tsx
  </files>
  <action>
    1. Extend `apps/site/src/lib/sandbox-storage.ts`:
       - Add `paused?: boolean` and `pausedAt?: number` fields to `SavedSandbox` interface
       - Add `updateSandboxPaused(paused: boolean): void` -- loads existing, updates paused/pausedAt, saves back
       - Existing loadSandbox/saveSandbox/clearSandbox remain backward compatible

    2. Create `apps/site/src/components/cloud/IdleWarningBanner.tsx`:
       - Fixed-position banner at top of viewport (z-50), yellow/amber background
       - Props: `secondsRemaining: number`, `onExtend: () => void`, `onDismiss: () => void`
       - Shows countdown text: "Sandbox will pause in {mm:ss} due to inactivity"
       - "Stay Active" button calls onExtend (sends heartbeat to reset timer)
       - Only renders when secondsRemaining > 0
       - Framer-motion slide-down animation (matching existing project style)

    3. Update `apps/site/src/components/cloud/SandboxRestore.tsx`:
       - Extend RestoreState: add `| { status: "paused"; sandboxId: string }`
       - In check(): if saved.paused is true, set status to "paused" without trying to connect
       - If saved.paused is false but checkSandbox returns not alive, try checking if it's paused (call `/api/sandbox/status?id=...` which now returns paused state)
       - Add "Resume Paused Sandbox" button that calls POST /api/sandbox/resume, shows loading spinner, then redirects to returned URL
       - On resume failure: show error message, offer "Create New Sandbox" option

    4. Update `apps/site/src/components/cloud/CloudLauncher.tsx`:
       - After sandbox is launched and user is redirected, start heartbeat interval:
         - Every 60 seconds, POST /api/sandbox/heartbeat with { sandboxId }
         - If response is "paused_or_dead", stop heartbeat and show notification
       - Track idle locally: reset a 15-minute countdown on any user interaction (mousemove, keydown, click, scroll -- debounced to 1 event per 5s)
       - When 13 minutes of idle (2 minutes before pause): show IdleWarningBanner with countdown
       - When "Stay Active" clicked: send heartbeat, reset idle timer, hide banner
       - 24-hour max lifetime: track sandbox creation time, show warning at 23h ("Sandbox will shut down in 1 hour, please save your work"), force pause at 24h via POST /api/sandbox/pause
       - On pause (auto or forced): call updateSandboxPaused(true), show message to user

    Note: The heartbeat + idle detection logic runs in the portal site (apps/site), NOT inside the CodePilot sandbox. The portal page that launched/redirected the user may be closed -- so heartbeat should also be sent from inside the sandbox via a lightweight script. However, per CONTEXT.md the decision is "CodePilot frontend sends heartbeat to portal API". Since the user is redirected to the sandbox URL and the portal tab may close, implement heartbeat as: (a) portal CloudLauncher sends heartbeat while its tab is open, AND (b) add a note that a future enhancement could add in-sandbox heartbeat. For now, rely on E2B's own timeout as the fallback if the portal tab is closed.
  </action>
  <verify>
    <automated>cd /home/user_demo/Husky/Codepilot-3 && npm run test</automated>
  </verify>
  <done>
    - SavedSandbox interface includes paused/pausedAt fields
    - IdleWarningBanner renders countdown with "Stay Active" button
    - SandboxRestore detects paused sandboxes and offers resume
    - CloudLauncher sends heartbeat every 60s, shows idle warning at 13min, triggers pause at 15min
    - 24-hour max lifetime warning and forced pause implemented
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Status API enhancement + cleanup logic</name>
  <files>
    apps/site/src/app/api/sandbox/status/route.ts
  </files>
  <action>
    1. Update `apps/site/src/app/api/sandbox/status/route.ts`:
       - Import `Sandbox` from "e2b" directly for listing
       - Enhanced response: `{ alive: boolean; ready: boolean; paused: boolean; url?: string; age?: number }`
       - To detect paused state: if `checkSandbox` returns not alive, call `Sandbox.list()` and check if sandboxId appears in the list -- if yes, it's likely paused (E2B lists paused sandboxes)
       - If sandbox is paused, return `{ alive: false, ready: false, paused: true }`
       - If sandbox truly doesn't exist, return `{ alive: false, ready: false, paused: false }`
       - Add `age` field: calculate from sandbox metadata or from the createdAt stored in localStorage (passed as query param)

    2. Lazy cleanup of expired sandboxes: when status check finds a sandbox older than 24 hours that's still running, call pauseSandbox to force-pause it. This is "cleanup on access" per Claude's discretion from CONTEXT.md.

    3. Consider adding a basic test for the paused-state detection logic (per checker feedback). If the detection logic is extracted into a pure function (e.g., `isPausedSandbox(sandboxId, listResult)`), write a small unit test in `apps/site/src/__tests__/sandbox-status.test.ts`. Otherwise, rely on typecheck + integration testing.
  </action>
  <verify>
    <automated>cd /home/user_demo/Husky/Codepilot-3 && npm run test</automated>
  </verify>
  <done>
    - Status API returns paused field distinguishing paused vs dead sandboxes
    - Expired sandboxes (>24h) are force-paused on status check
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
1. `npm run test` passes (typecheck + unit tests)
2. All UI components compile and export correctly
3. sandbox-storage.ts backward compatible (existing loadSandbox still works)
4. Status API returns paused field
</verification>

<success_criteria>
- Idle warning banner appears 2 minutes before auto-pause
- SandboxRestore distinguishes paused vs dead sandboxes and offers resume
- CloudLauncher sends heartbeat every 60s
- 24-hour max lifetime warning and forced pause implemented
- Status API returns paused state
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-persistence-hardening/03-02-SUMMARY.md`
</output>
